import { JsxElement } from "typescript";

/** Task Reminder Component
 * @returns {JsxElement} Task Reminder
 */
const Reminder = () => {
  return (
    <header>
      <h1>Task Tracker!</h1>
    </header>
  );
};

export default Reminder;
